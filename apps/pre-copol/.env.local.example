# URL do backend API.
# Local dev: http://137.131.233.254:3000 (HTTP direto no IP da VPS — ok
# porque o `next dev` local também roda em HTTP, sem mixed content).
# Produção (GitHub Pages, HTTPS): https://137-131-233-254.sslip.io —
# obrigatório usar HTTPS aqui, senão o navegador bloqueia a chamada por
# mixed content. Configurado como env do job de build, não neste arquivo
# (ver .github/workflows/pre-copol-pages.yml).
NEXT_PUBLIC_API_URL=http://137.131.233.254:3000
